# Kopiere diese Datei nach .env und passe die Werte an.
# Die .env-Datei wird von der Anwendung automatisch geladen (nur lokal – ignoriert in CF).

# Pflichtfelder: AES-256-GCM-Schlüssel + Salt für Token- und Passwort-Verschlüsselung.
# Salt muss ein fester 16-Byte-Hex-Wert (32 Hex-Zeichen) sein und über Deployments
# hinweg KONSTANT bleiben – sonst sind gespeicherte Tokens nach einem Neustart unlesbar.
# Generieren: openssl rand -hex 16
TOKEN_ENCRYPTION_KEY=change-me-32-chars-minimum-secret!
TOKEN_ENCRYPTION_SALT=change-me-use-openssl-rand-hex-16

# Backup Manager (Staging / lokale Zielumgebung)
MANAGER_LOCAL_URL=https://backup-manager-staging.example.com
AGENT_LOCAL_URL=https://backup-agent-staging.example.com
AGENT_LOCAL_USER=admin
AGENT_LOCAL_PASS=admin

# CloudFoundry Service Account (für Token-Abruf)
UAA_LOCAL_ENDPOINT=https://uaa.sys-staging.example.com
CF_API_LOCAL_ENDPOINT=https://api.sys-staging.example.com
SA_LOCAL_USERNAME=cf-backup-monitor-sa
SA_LOCAL_PASSWORD=change-me

# CF-Space für Auto-Provisioning (cf space <name> --guid)
LOCAL_CF_SPACE_GUID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Service-Instanz
LOCAL_INSTANCE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# S3-Service für Auto-Provisioning (pro DB-Instanz konfigurierbar)
# LOCAL_S3_INSTANCE_NAME=my-backup-s3
# LOCAL_S3_SERVICE_PLAN=5gb        # Nur nötig wenn S3-Instanz automatisch angelegt werden soll

# Lokale Sandbox-Datenbank (für Restore-Tests)
SANDBOX_LOCAL_USER=sandbox_user
SANDBOX_LOCAL_PASS=change-me

# Actuator-Zugangsdaten (für Prometheus-Scraping und Health-Details)
# Prometheus muss diese Credentials als HTTP Basic Auth mitsenden.
ACTUATOR_USERNAME=prometheus
ACTUATOR_PASSWORD=change-me

# Auto-Provisioning: Backup-Plan automatisch anlegen wenn kein Plan existiert
# Setzt AUTO_PROVISION_ENABLED=true und MANAGER_1_CF_SPACE_GUID voraus.
# AUTO_PROVISION_ENABLED=false
# AUTO_PROVISION_S3_LABEL=s3
# AUTO_PROVISION_SCHEDULE=0 2 * * *
# MANAGER_1_CF_SPACE_GUID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
